agentkit-sdk-python==0.5.5
# Keep the current VeADK runtime.  It includes the generic AgentKit Sandbox
# runner, which forwards SKILL_SPACE_ID to the associated Skills Sandbox.
veadk-python==0.5.40
# AgentKit 0.5.5 imports the A2A 0.x Starlette application path.  Without an
# upper bound a clean image build can resolve a2a-sdk 1.x and fail at startup.
a2a-sdk>=0.3.7,<1.0
# Keep FastAPI/Starlette in the lifecycle range used by AgentKit 0.5.5.
fastapi==0.124.1
starlette==0.49.1
# Required by VeADK ShortTermMemory(backend="postgresql") when the AgentKit
# session-management component injects DATABASE_POSTGRESQL_*.
psycopg[binary]>=3.2,<4
# Used by the customer-service Runtime to discover a remote A2A Agent Card
# and send a standard JSON-RPC ``message/send`` delegation request.
httpx>=0.27,<1
# Used directly by the local BFF and Knowledge/MEM0 adapters.
requests>=2.31,<3
